<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="BottomNavigationViewTheme">
        <item name="android:background">@color/colorBottomNavMenuBackground</item>
        <item name="itemTextColor">@color/colorBottomNavMenuText</item>
        <item name="itemIconTint">@color/colorBottomNavMenuText</item>
    </style>

</resources>
